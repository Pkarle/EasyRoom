{% extends 'layouts/front.html.twig' %}

{% block title %}Create property{% endblock %}

{% block body %}
    <h1>Create reservation</h1>

    {{ form_start(createReservationForm) }}
    {{ form_row(createReservationForm.dateStart) }}
    {{ form_row(createReservationForm.dateEnd) }}
    {{ form_row(createReservationForm.numberPersons) }}
    {{ form_row(createReservationForm.finalPrice) }}
    {{ form_row(createReservationForm.approuved) }}

    <button class="btn btn-primary">Create</button>
    {{ form_end(createReservationForm) }}

{% endblock %}